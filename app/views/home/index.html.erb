<!DOCTYPE html>
<html>
<head>
  <title>Welcome to the Home Page</title>
</head>
<body>
  <div class="container" style="font-family: Arial, sans-serif; background-color: #f2f2f2; margin: 0; padding: 0; display: flex; justify-content: center; align-items: center; height: 50vh;">
    <% if !user_signed_in? %>
      <div class="box sign-in" style="max-width: 500px; padding: 40px; background-color: #8b0000; box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); border-radius: 8px; text-align: center;">
        <h1 style="color: #f2f2f2; margin-top: 0;">Sign In</h1>
      </div>
    <% else %>
      <div class="box user-info" style="max-width: 500px; padding: 40px; background-color: #FFFFFF; box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); border-radius: 8px; text-align: center;">
        <h1 style="color: #8b0000; margin-top: 0;">Welcome to the Home Page!</h1>
        <p style="margin-bottom: 20px; color: #555;">Hello, <%= current_user.email %></p>
        <div class="links" style="display: flex; justify-content: space-between; margin-top: 0px;">
          <%= link_to "Sign out", destroy_user_session_path, method: :delete, style: "padding: 10px 20px; color: #f2f2f2; background-color: #8b0000; text-decoration: none; border-radius: 4px;" %>
          <%= link_to "Change password", edit_user_registration_path, style: "padding: 10px 20px; color: #f2f2f2; background-color: #8b0000; text-decoration: none; border-radius: 4px;" %>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
