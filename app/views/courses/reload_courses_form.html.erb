<!DOCTYPE html>
<html>
<head>
  <title>Reload Courses</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0; 
    }

    .container {
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 800px; 
      height: auto; 
      margin: 0 auto; 
      box-sizing: border-box; 
      overflow: auto; 
    }

    h1 {
      text-align: center;
    }

    .field {
      margin-bottom: 15px;
    }

    label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
    }

    select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }

    .actions {
      text-align: center;
      margin-top: 15px;
    }

    .actions input[type="submit"] {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
    }

    .actions input[type="submit"]:hover {
      background-color: #45a049;
    }

    .checkbox-container {
      display: flex;
      align-items: center;
    }

    .checkbox-container label {
      margin-bottom: 0;
      margin-left: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Reload Courses</h1>
    <%= form_tag(load_courses_courses_path, method: :get) do %>

      <div class="field">
        <%= label_tag(:term, 'Term') %>
        <%= select_tag(:term, options_for_select({ 'Autumn 2023' => '1238', 'Spring 2024' => '1242' })) %>
      </div>

      <div class="field">
        <%= label_tag(:campus, 'Campus') %>
        <%= select_tag(:campus, options_for_select({ 'Columbus' => 'col', 'Lima' => 'lma', 'Mansfield' => 'mns', 'Marion' => 'mrn', 'Newark' => 'nwk', 'Wooster' => 'wst' })) %>
      </div>

      <div class="field">
        <%= label_tag(:career, 'Career') %>
        <%= select_tag(:career, options_for_select({ 'Undergraduate' => 'ugrd', 'Graduate' => 'grad' })) %>
      </div>

      <div class="actions">
        <%= submit_tag 'Load Courses' %>
      </div>
    <% end %>
  </div>
</body>
</html>
