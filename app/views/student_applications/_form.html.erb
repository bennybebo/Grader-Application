<!DOCTYPE html>
<html>
<head>
  <title>Submit Application</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0; 
    }

    .container {
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 800px; 
      height: auto; 
      margin: 0 auto; 
      box-sizing: border-box; 
      overflow: auto; 
    }

    h1 {
      text-align: center;
    }

    .field {
      margin-bottom: 15px;
    }

    label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
    }

    input[type="text"],
    textarea {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }

    .actions {
      text-align: center;
      margin-top: 15px;
    }

    .actions input[type="submit"] {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
    }

    .actions input[type="submit"]:hover {
      background-color: #45a049;
    }

    .error {
      color: #f44336;
      margin-top: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <%= form_with(model: @student_application, url: student_applications_path, method: :post) do |form| %>
      <%= form.hidden_field :class_number, value: @class_number %>
      <%= form.hidden_field :email, value: current_user.email %>

      <div class="field">
        <%= form.label :application_text, 'Why do you want to be a grader for this section?' %>
        <%= form.text_area :application_text, rows:20, placeholder: 'Enter your answer here...' %>
      </div>

      <div class="actions">
        <%= form.submit 'Submit' %>
      </div>
    <% end %>
  </div>
</body>
</html>